{
  "description": "Keeps C# edits on the validate_patch write path, keeps C# reads on search_index/get_symbol, and reminds Claude to refresh the workspace after creating a new .cs file",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/guard-cs-edit.sh",
            "timeout": 10,
            "statusMessage": "Checking C# write path..."
          }
        ]
      },
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/guard-cs-read.sh",
            "timeout": 10,
            "statusMessage": "Checking C# read path..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/hint-reload-new-cs-file.sh",
            "timeout": 10,
            "statusMessage": "Checking whether the workspace needs a reload..."
          }
        ]
      }
    ]
  }
}
