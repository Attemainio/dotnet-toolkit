# Devlog 2026-W29

## 2026-07-13 — Initial implementation of dotnet-toolkit MCP server plugin
<!-- devlog {"id":"20260713-0439-c8b4","ts":"2026-07-13T04:39:11.6276218+03:00","status":"done","classes":["Program","SolutionLocator","WorkspaceHost","SymbolResolver","SlnxParser","ProjectIndex","OutlineBuilder","DevlogStore","DevlogParser","DevlogSearch","CompactFormatter","OutlineRenderer","ServerTools","StructureTools","RoslynTools","DevlogTools"],"ensemble":"McpServer","tags":["bootstrap","mcp","roslyn","plugin"]} -->

**WHAT:** Built the full Claude Code plugin: stdio MCP server (ModelContextProtocol 1.4.1, net10.0) with 14 tools across four domains (server status, Roslyn semantic queries, structure index, devlog), three skills, plugin manifest, and 27 passing tests including MSBuildWorkspace integration tests against a fixture solution.

**WHY:** Reduce Claude's token usage on .NET repos: outlines measured at 5-10% of raw file size; semantic find_references replaces grep; devlog search replaces re-reading history.

**OBSERVATIONS:** Two-tier design (syntax index available instantly, MSBuildWorkspace loaded in background) satisfies the ~5s MCP startup timeout. MSBuildLocator requires an explicit Microsoft.Build.Framework reference with ExcludeAssets=runtime (error MSBL001 otherwise). Roslyn 5.6 obsoleted Workspace.WorkspaceFailed in favor of RegisterWorkspaceFailedHandler. MSBuildWorkspace 5.6 opens .slnx natively (fallback SlnxParser kept anyway). FileSystemWatcher does not work on /mnt/c (WSL DrvFs) so invalidation is mtime-polling. .mcp.json launches via scripts/run-server.sh which prefers ~/.dotnet so the plugin runs even when the system dotnet is v8.

**FIX:** dotnet test: 27/27 green. JSON-RPC handshake smoke test: tools/list returns all 14 tools, stdout protocol-only, workspace loads this repo's slnx (2 projects) in 1.4s.

